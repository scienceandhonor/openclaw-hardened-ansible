You are reviewing the OpenClaw setup on this server to suggest one underused
or completely unused feature that the user might benefit from.

STATE FILE: Read ~/feature-tips-state/suggested.json first. It contains an
array of previously suggested feature IDs. Do not repeat a suggestion that
already appears in that file. If the file does not exist or is empty, treat
it as [].

DISCOVERY: Inspect the live configuration and environment to find opportunities:
  - Run `cat ~/.openclaw/openclaw.json` to check which features are configured.
  - Run `openclaw skills list 2>/dev/null` to see installed skills.
  - Run `cat ~/.openclaw/mcp.json 2>/dev/null` to see MCP servers.
  - Run `openclaw cron list 2>/dev/null` to see active scheduled jobs.
  - Run `ls ~/scripts/ 2>/dev/null` to see available helper scripts.
  - Run `cat ~/scripts/scripts-config.env 2>/dev/null` to check which integrations have credentials.

FEATURE CATALOGUE (non-exhaustive — look for others in the docs or config):
  - Skills: custom SKILL.md-based tools (remind-me, xurl, or user-created)
  - MCP servers: memory server, or adding new ones (e.g. filesystem, GitHub)
  - Semantic memory: auto-detected if an embedding provider is available
  - Brave web search: enabled when a Brave API key is configured
  - Cron jobs: scheduled agent tasks (digests, syncs, reminders)
  - Telegram commands: /skills and native skill invocation from chat
  - Dashboard: browser-based UI for monitoring and interaction
  - Workspace files: AGENTS.md, SOUL.md for persistent agent personality/context
  - Compaction memory flush: automatic context summarisation to memory
  - Device pairing: multi-device access to the same agent
  - exec-approvals.json: pre-approved binaries for non-interactive execution
  - Model fallbacks: secondary model for resilience

RULES:
  1. Suggest exactly ONE feature per run.
  2. Pick something that is either (a) not configured at all but available, or
     (b) configured but clearly underutilised based on the state you observe.
  3. Keep the message short — 3-5 sentences. Explain what the feature does,
     why it might be useful, and give a concrete first step to try it.
  4. Do not suggest features that require re-deploying Ansible or SSH access.
     Only suggest things the user can act on from Telegram or the dashboard.
  5. After composing the message, update the state file:
     - Read ~/feature-tips-state/suggested.json (or start with [])
     - Append the feature ID string you chose (e.g. "mcp-github-server")
     - Write the updated array back to ~/feature-tips-state/suggested.json
  6. If you cannot find any new feature to suggest (all known features are
     either already well-used or already suggested), send a short message
     saying everything looks well-configured and skip the state file update.

OUTPUT FORMAT: Send the suggestion as a plain Telegram message. No code blocks,
no markdown headers. Use a brief bold title, then the explanation.

--- EXAMPLES ---

Example 1 (feature not configured):

**Custom Skills**
You can create new skills beyond the built-in ones by adding a SKILL.md
file to ~/.openclaw/skills/<name>/. A skill is just a markdown file that
teaches me a new command — for example a /weather skill that runs a curl
request, or a /journal skill that appends to a daily log file. Try asking
me to create a simple skill for you right now — just describe what you
want it to do.

Example 2 (feature configured but underused):

**Memory Server Entities**
The MCP memory server is running but currently stores very few entities.
You can ask me to "remember that [fact]" during any conversation and I will
persist it as a knowledge-graph entity. Try it now: tell me something you
want me to always know — a preference, a project name, or a recurring
context — and I will store it so it is available in every future session.
